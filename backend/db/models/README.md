# Database Models

Place all Mongoose models here.

Example structure:
models/
├── user/
│ ├── userModel.js
│ └── userMethods.js
└── task/
└── taskModel.js

**models/Task is simply an example of what could be done, it is optional to keep**
